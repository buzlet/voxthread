{
  "name": "voxthread",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:userscript": "node scripts/build-userscript.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
